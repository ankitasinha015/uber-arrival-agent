"""Tool: estimate_eta — REAL (Maps API).

Airport -> hotel travel time. Used to convert "ride started" into a tight
room-arrival estimate. Free credit; key in MAPS_API_KEY.

Implementation: TODO.
"""

from __future__ import annotations


def estimate_eta(origin: str, destination: str) -> int:
    """Return estimated travel minutes from `origin` to `destination` with live
    traffic. Returns minutes (int). Shape/precision: TODO.
    """
    raise NotImplementedError
