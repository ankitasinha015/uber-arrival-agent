# Environment
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local scratch
*.log
scratch/

# Taste store (file-on-disk persistence — pickle per user). Seeded data for
# the deployed demo lives in scenarios/cache/ via the cache-capture step.
.taste/
.chroma/

# Personal interview-prep doc — lives outside the repo at
# ~/.gstack/projects/uber-arrival-agent/interview-prep.md
docs/interview-prep.md
serve_local.py
.gstack/

# behaviour memory (per-user counts)
.memory/

# LangGraph SQLite checkpointer (durable trip state)
concierge_checkpoints.sqlite
*.sqlite
